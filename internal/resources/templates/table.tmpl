// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

{{ $tablename := .TableName}}
package tbl{{ $tablename }}

import (
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
)

var (
{{- range $key, $value := .Columns}}
	{{ index $value 0 }} = dialect.Field{Name: "{{index $value 1}}",Json: "{{index $value 2}}",Table: "{{ $tablename }}",Type: "{{index $value 3}}"}
{{- end}}

{{- if .HasPrimaryKey}}
	// 主键
	PrimaryKey = {{index .PrimaryKey 0}}
{{- end}}

	// 可写列
	WritableFields = []dialect.Field{
{{- range $key, $value := .Columns}}
	{{- if ne (getType $value) "any"}}
	{{- if or (eq (index $value 4) "->") (eq (index $value 4) "")}}
	{{index $value 0}},
	{{- end}}
	{{- end}}
{{- end}}
	}
	// 可读列
	ReadableFields = []dialect.Field{
{{- range $key, $value := .Columns}}
	{{- if ne (getType $value) "any"}}
	{{- if or (eq (index $value 4) "<-") (eq (index $value 4) "")}}
	{{index $value 0}},
	{{- end}}
	{{- end}}
{{- end}}
	}

)